# These settings were set by the catalyst build script that automatically
# built this stage.
JOBS="8"
FLTO="-flto=${JOBS}"
MARCH="-march=native"
COMMON_FLAGS="${FLTO} ${MARCH} -ftree-vectorize -pipe -O2"

CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
MAKEOPTS="-j${JOBS}"

# NOTE: This stage was built with the bindist Use flag enabled
#-- [NECESSARY] elogind -systemd, udev, dbus, policykit, udisks, X
USE="bluetooth dbus elogind git policykit udev udisks X xinerama -systemd -wayland"
VIDEO_CARDS="amdgpu"
INPUT_DEVICES="libinput synaptics"

## Emerge list (M: menually compile, not by ebuild)
#-- [VIEW] WM
# openbox nitrogen lxappearance obconf obmenu-generator plank(M) tint2 picom dunst

#-- [SYS] System configurations
# xrandr pulseaudio brightnessctl::guru

#-- [APPS] System apps
# arandr pavucontrol thunar thunar-volman thunar-archive-plugin xfce4-power-manager hardinfo

#-- [FONT] System fonts
# jetbrainsmono-nerdfont::guru nanum ibus ibus-libpinyin ibus-hangul

#-- [APPS] User apps
# kitty neovim vscodium viewnior mpv mpd mpc ncmpcpp gimp inkscape
# htop neofetch


# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C
LINGUAS="ko"
ACCEPT_LICENSE="*"

# For installing gentoo
GENTOO_MIRRORS="http://ftp.daum.net/gentoo/ http://ftp.kaist.ac.kr/pub/gentoo/ ftp://ftp.kaist.ac.kr/gentoo/ http://ftp.lanet.kr/pub/gentoo/ https://ftp.lanet.kr/pub/gentoo/ ftp://ftp.lanet.kr/pub/gentoo/"
GRUB_PLATFORMS="efi-64"
